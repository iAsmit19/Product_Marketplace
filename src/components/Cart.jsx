import { cartContainer } from "../stylesheets/cart.module.css";

const Cart = () => {
  const vid_time = 18.23;
  console.log(vid_time);
  
  return (
    <div className={cartContainer}>
      Cart
    </div>
  );
};

export default Cart;
